import SectionCards from "@/components/sectionCards";
import SectionHero from "@/components/sectionHero";

const Home = () => {
	return (
		<>
			<SectionHero />
			<SectionCards />
		</>
	);
};

export default Home;
